# fizzy

## Config
- `cd` vào `auth-server` chạy `bun install`, sau đó `bun dev`
  - Trước khi chạy `auth-server` lần đầu sẽ cần `bunx prisma db pull`
  - **Đừng chạy** `bunx prisma db push`!!
- `cd` vào `web-app` chạy `bun install`, sau đó `bun dev`
